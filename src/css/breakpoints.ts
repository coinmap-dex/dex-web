import {BREAKPOINT_WIDTH} from '../constants/breakpoint.constants';

export default {
    xs: `(max-width: ${BREAKPOINT_WIDTH.XS}px)`,
    sm: `(min-width: ${BREAKPOINT_WIDTH.SM}px)`,
    md: `(min-width: ${BREAKPOINT_WIDTH.MD}px)`,
    lg: `(min-width: ${BREAKPOINT_WIDTH.LG}px)`,
    xl: `(min-width: ${BREAKPOINT_WIDTH.XL}px)`,
    xxl: `(min-width: ${BREAKPOINT_WIDTH.XXL}px)`
}
